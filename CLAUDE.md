# گوهر ولا (Goharevela)

فروشگاه آنلاین جواهرات نقره. بک‌اند Django + Wagtail، فرانت‌اند Next.js 14.

## استقرار (Deployment)

> ⚠️ **مهم:** پروداکشن کاملاً با **systemd** اجرا می‌شود، **نه Docker و نه PM2**.
> فایل‌های `Dockerfile` و `frontend/ecosystem.config.js` در استقرار واقعی استفاده **نمی‌شوند**.

- **سرور:** VPS، مسیر ریپو `/var/www/goharevela` (دامنه `goharevela.ir`)
- **اسکریپت استقرار:** `./deploy.sh` (روی خود سرور اجرا می‌شود)
- **فرانت‌اند:** با **systemd** اجرا می‌شود
  - سرویس: `goharevela-frontend` (در صورت تفاوت: `FRONTEND_SERVICE=نام ./deploy.sh`)
  - دستور: `npm run build` سپس `sudo systemctl restart goharevela-frontend`
  - حالت `next start` است (نه dev) — پس هر تغییر فرانت نیاز به **build مجدد** دارد.
- **بک‌اند:** با **systemd** اجرا می‌شود
  - سرویس: `gunicorn` (در صورت تفاوت: `BACKEND_SERVICE=نام ./deploy.sh`)
  - تنظیمات: `config.settings.production`
  - دستور: `migrate` + `collectstatic` سپس `sudo systemctl restart gunicorn`

## نکات مهم

- **دیتابیس، `.env` و `media/`** فقط روی سرور هستند و در `.gitignore` قرار دارند —
  هیچ‌وقت در git کامیت نشوند (دیتابیس واقعی نباید overwrite شود).
- **اسم برند در دیتابیس:** عنوان بنر صفحه اصلی و اسم Site در Wagtail داخل دیتابیس
  ذخیره می‌شوند و با استقرار کد عوض **نمی‌شوند**؛ در صورت نیاز دستی از پنل `/cms/` ویرایش شوند.
- `WAGTAIL_SITE_NAME` در `backend/config/settings/base.py` تعریف شده (تنظیمات کد، نه دیتابیس).

## پرداخت، پیامک و ارسال (بدون نیاز به کد)

همه از پنل `/cms/` بخش **تنظیمات** و **اسنیپت‌ها** قابل مدیریت‌اند:

- **درگاه پرداخت** (تنظیمات → «تنظیمات پرداخت»): فعال/غیرفعال کردن پرداخت آنلاین،
  انتخاب درگاه (زرین‌پال یا آی‌دی‌پی)، وارد کردن Merchant ID/API Key، حالت
  آزمایشی، و اطلاعات کارت به کارت (شماره کارت و نام صاحب کارت).
  - وقتی پرداخت آنلاین فعال و کلید وارد شده باشد، آداپتور مربوطه در زمان اجرا
    انتخاب می‌شود؛ در غیر این صورت فقط کارت به کارت نمایش داده می‌شود.
  - مسیر بازگشت درگاه: `FRONTEND_URL` در `.env` تنظیم شود (پیش‌فرض `https://goharevela.ir`).
- **تأیید پیامکی** (تنظیمات → «تنظیمات پیامک»): فعال‌سازی، انتخاب سرویس
  (کاوه‌نگار یا اس‌ام‌اس.آی‌آر)، API Key، شماره فرستنده و نام قالب‌ها.
  کد OTP در Redis cache نگهداری می‌شود (۵ دقیقه). تا وقتی پیامک فعال نشده،
  کد فقط در لاگ ثبت می‌شود و پیامکی ارسال نمی‌گردد.
- **روش‌های ارسال** (اسنیپت‌ها → «روش‌های ارسال»): افزودن/ویرایش روش ارسال با
  نام، هزینه، آستانه ارسال رایگان و زمان تحویل. در صفحه تسویه‌حساب نمایش داده
  می‌شوند و هزینه ارسال از روی انتخاب کاربر محاسبه می‌شود.

**مقداردهی اولیه (یک‌بار روی سرور):**
```bash
python manage.py seed_commerce --settings=config.settings.production
```
این دستور اطلاعات کارت به کارت و دو روش ارسال پیش‌فرض را می‌سازد (ایمن و
idempotent — برای بازنویسی `--force`).
